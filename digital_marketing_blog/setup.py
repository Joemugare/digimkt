from setuptools import setup, find_packages

setup(
    name="digital_marketing_blog",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # list your dependencies here, e.g.,
        # "Django>=4.0",
        # "requests"
    ],
)
